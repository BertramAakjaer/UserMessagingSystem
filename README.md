# User Messaging Secury system
*Currently the secret keys is stored in the "database"(users.json), and this wouldn't be the reality IRL, but for this local demo it is*

Run the code with:
- `pip install -r requirements.txt`
- `python main.py`